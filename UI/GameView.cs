using FoxTrader.UI.Control;

namespace FoxTrader.UI
{
    internal class GameView : Panel
    {
        protected GameView(GameControl c_controlParent) : base(c_controlParent)
        {
        }
    }
}